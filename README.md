# iBookRead

<div align="center">

📚 一个轻量级、功能强大的命令行文档阅读工具

[![Python Version](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![Version](https://img.shields.io/badge/version-2.0.0-blue.svg)](https://github.com/yourusername/iBookRead)

支持 EPUB、MOBI、TXT、Markdown 等多种文档格式

</div>

---

## ✨ 特性

- 📖 **多格式支持** - 支持 EPUB、MOBI、TXT、Markdown 等常见文档格式
- 📤 **纯文本输出** - 默认直接输出全文，方便管道处理和脚本集成
- 📄 **智能分页** - 内置交互式分页器，支持 vim 风格导航
- 🎯 **精准定位** - 支持按页码、章节、百分比跳转，可查看完整文档
- 💾 **进度保存** - 自动保存阅读进度，下次打开自动恢复
- 🔒 **密码保护** - 可选的密码保护功能，保护隐私
- 🌏 **中英文支持** - 完美支持中英文混排和多种编码
- ⚡ **管道友好** - 可与其他命令行工具无缝配合
- 🚀 **高性能** - 优化的分页算法，大文档秒开
- 🔇 **静默模式** - 无干扰输出，退出分页器后屏幕干净
- 🖥️ **跨平台** - 支持 Linux、macOS、Windows

---

## 📦 安装

### 方式一：使用 pip 安装（推荐）

```bash
pip install ibook-reader
```

### 方式二：从源码安装

```bash
# 克隆仓库
git clone https://github.com/yourusername/iBookRead.git
cd iBookRead

# 安装
pip install .
```

### 方式三：使用 wheel 包安装

```bash
pip install ibook_reader-1.0.0-py3-none-any.whl
```

---

## 🚀 快速开始

### 基本用法

```bash
# 默认阅读（自动分页，自动恢复上次进度）
ibook book.epub

# 跳转到指定章节（加载完整文档，可前后翻页）
ibook --chapter 5 book.epub

# 跳转到指定页码
ibook --page 100 book.epub

# 跳转到 50% 位置
ibook --percent 50 book.epub

# 管道模式：限制输出页数
ibook --chapter 5 --pages 10 book.epub | cat

# 输出到文件
ibook book.epub > book.txt

# 与其他命令配合使用
ibook book.epub | grep "关键词"
ibook book.epub | head -50
ibook book.epub | wc -l
```

### 首次使用

**基本阅读**：

```bash
$ ibook book.epub
# 使用内置交互式分页器
# 底部显示：1-20/1000 (2%) | 空格/b:翻页 j/k:滚动 g/G:首/尾 q:退出
# 退出时自动保存进度
```

**再次打开（自动恢复进度）**：

```bash
$ ibook book.epub
# 自动从上次阅读位置继续
# 可以向前/向后翻页查看任意内容
```

**首次运行设置密码**（可选）：

```bash
$ ibook book.epub
请设置密码（留空跳过）: ****
请再次输入密码: ****
✓ 密码设置成功

# 文档内容...
```

**跳过密码快速查看**：

```bash
$ ibook book.epub
# 输入密码后直接显示内容
```

---

## ⌨️ 分页器快捷键

在终端中阅读时，iBookRead 使用内置分页器，**支持实时进度追踪**。以下是常用快捷键：

| 按键 | 功能 | 说明 |
|-----|------|------|
| `Space` / `f` | 下一页 | 向下翻一整页 |
| `b` | 上一页 | 向上翻一整页 |
| `j` / `Enter` | 下一行 | 向下滚动一行 |
| `k` | 上一行 | 向上滚动一行 |
| `g` | 跳到开头 | 跳转到文档开头 |
| `G` | 跳到结尾 | 跳转到文档结尾 |
| `q` | 退出 | 退出阅读（自动保存实际进度） |

> **实时进度追踪**：分页器会记录您的每次翻页操作，退出时自动保存实际阅读位置！

---

## 🔧 命令行选项

```bash
ibook [选项] [文件路径]
```

### 选项说明

| 选项 | 说明 |
|-----|------|
| `-h`, `--help` | 显示帮助信息 |
| `--version` | 显示版本号 |
| `--set-password` | 设置或修改密码（已有密码需先验证） |
| `--reset-password` | 重置密码（清除现有密码） |
| `--page N` | 从指定页码开始输出到末尾 |
| `--chapter N` | 从指定章节开始输出到末尾（章节从0开始计数） |
| `--percent N` | 从指定百分比进度开始输出到末尾（0-100） |
| `--pages N` | 限制输出页数（配合跳转参数使用） |
| `--clean` | 清理所有数据（配置、进度、书签） |

### 使用示例

```bash
# 查看版本
ibook --version

# 查看帮助
ibook --help

# 阅读整本书
ibook book.epub

# 从第10章开始读到末尾（章节从0开始）
ibook --chapter 10 book.epub

# 从第100页开始读到末尾
ibook --page 100 book.epub

# 从50%进度开始读到末尾
ibook --percent 50 book.epub

# 只输出10页（从第5章开始）
ibook --chapter 5 --pages 10 book.epub

# 导出到文件
ibook book.epub > output.txt

# 搜索关键词
ibook book.epub | grep "关键词"

# 统计字数
ibook book.epub | wc -w

# 查看前100行
ibook book.epub | head -100

# 设置或修改密码
ibook --set-password

# 重置密码（清除现有密码）
ibook --reset-password

# 清理所有数据
ibook --clean
```

---

## 📖 输出格式说明

### 完整输出模式

当不使用跳转参数时，输出完整文档：

```
================================================================================
第一章 标题
================================================================================

章节内容...

================================================================================
第二章 标题
================================================================================

章节内容...
```

### 跳转输出模式

使用 `--page`、`--chapter` 或 `--percent` 时：
- **终端模式**：加载完整文档，自动跳转到指定位置，可前后翻页
- **管道模式**：从跳转位置输出到文档末尾
- **限制输出**：管道模式下使用 `--pages N` 可以只输出指定页数

```bash
# 跳转到50%位置，但可以查看整个文档
ibook --percent 50 book.epub
# 状态栏显示：5001-5020/10000 (50%) | ...
# 按 b 可以向前翻页

# 管道模式：从第2章开始只输出5页
ibook --chapter 2 --pages 5 book.epub | cat
```

### 管道输出

所有操作均需要密码验证，包括管道操作：

```bash
# 提取包含关键词的内容（需要密码验证）
ibook book.epub | grep "关键词"

# 统计总字数
ibook book.epub | wc -w

# 保存到文件
ibook book.epub > book.txt

# 查看前50行
ibook book.epub | head -50
```

---

## 📁 数据存储

所有配置数据存储在用户目录下：

```
~/.ibook_reader/
├── config.json          # 配置文件（密码哈希等）
├── progress.json        # 阅读进度（按文件哈希索引）
└── bookmarks/           # 书签目录
```

- **Linux/macOS**: `~/.ibook_reader/`
- **Windows**: `%USERPROFILE%\.ibook_reader\`

### 进度文件示例

```json
{
  "documents": [{
    "file_path": "/path/to/book.epub",
    "file_hash": "468e3ee38544e1e278264097e81a866e",
    "current_page": 100,
    "current_chapter": 5,
    "total_pages": 520,
    "read_percentage": 19.2,
    "last_read_time": "2024-11-17T20:00:00"
  }]
}
```

**特性**：
- 使用绝对路径存储，任意目录访问都能匹配
- 按文件哈希索引，移动文件后进度仍然保留
- 自动清理30天未读的旧进度记录

---

## 🔒 安全性

### 密码保护

iBookRead 使用 **SHA-256** 算法加密存储密码：

- 密码经过哈希处理，不以明文存储
- 每个密码都有唯一的随机盐值
- 密码验证失败3次后自动退出
- 支持随时重置密码

### 隐私保护

- 所有数据都存储在本地
- 不会收集或上传任何用户数据
- 可随时使用 `--clean` 清理所有数据

---

## 🎯 支持的文件格式

| 格式 | 扩展名 | 支持程度 | 说明 |
|-----|-------|---------|------|
| **EPUB** | `.epub` | ✅ 完全支持 | 提取文本内容，支持章节导航 |
| **MOBI** | `.mobi`, `.azw` | ✅ 完全支持 | 提取文本内容 |
| **TXT** | `.txt` | ✅ 完全支持 | 自动检测编码（UTF-8/GBK/GB2312等） |
| **Markdown** | `.md`, `.markdown` | ✅ 完全支持 | 按一级标题分章节 |

---

## 🛠️ 开发

### 环境要求

- Python 3.8 或更高版本
- pip

### 安装开发依赖

```bash
# 克隆仓库
git clone https://github.com/yourusername/iBookRead.git
cd iBookRead

# 创建虚拟环境
python -m venv venv
source venv/bin/activate  # Linux/macOS
# 或
venv\Scripts\activate     # Windows

# 安装开发依赖
pip install -e .
pip install pytest pytest-cov
```

### 运行测试

```bash
# 运行所有测试
pytest

# 运行测试并查看覆盖率
pytest --cov=ibook_reader --cov-report=term

# 运行特定测试
pytest tests/unit/test_parsers.py
```

### 构建发布包

```bash
# 安装构建工具
pip install build

# 构建
python -m build

# 生成的包在 dist/ 目录
ls dist/
# ibook_reader-1.0.0-py3-none-any.whl
# ibook_reader-1.0.0.tar.gz
```

---

## 📋 技术栈

- **语言**: Python 3.8+
- **EPUB解析**: [ebooklib](https://github.com/aerkalov/ebooklib)
- **MOBI解析**: [mobi](https://github.com/iscc/mobi)
- **Markdown解析**: [markdown](https://github.com/Python-Markdown/markdown)
- **编码检测**: [chardet](https://github.com/chardet/chardet)
- **HTML解析**: [BeautifulSoup4](https://www.crummy.com/software/BeautifulSoup/)
- **测试框架**: [pytest](https://pytest.org/)

---

## 🗺️ 项目结构

```
iBookRead/
├── ibook_reader/           # 主包目录
│   ├── __init__.py
│   ├── cli.py             # 命令行入口
│   ├── config.py          # 配置管理
│   ├── models/            # 数据模型
│   │   ├── document.py   # 文档模型
│   │   ├── bookmark.py   # 书签模型
│   │   └── progress.py   # 进度模型
│   ├── parsers/           # 文档解析器
│   │   ├── base.py       # 基础解析器
│   │   ├── epub_parser.py
│   │   ├── mobi_parser.py
│   │   ├── txt_parser.py
│   │   ├── markdown_parser.py
│   │   └── factory.py    # 解析器工厂
│   ├── core/              # 核心功能
│   │   ├── format_detector.py  # 格式检测
│   │   ├── paginator.py        # 分页引擎
│   │   └── interactive_pager.py # 交互式分页器
│   ├── services/          # 业务服务层
│   │   ├── auth_service.py     # 认证服务
│   │   ├── reader_service.py   # 阅读服务
│   │   ├── bookmark_service.py
│   │   └── progress_service.py
│   └── utils/             # 工具函数
│       ├── crypto.py     # 加密工具
│       ├── file_utils.py # 文件工具
│       └── text_utils.py # 文本工具
├── tests/                 # 测试目录
│   ├── unit/             # 单元测试
│   └── integration/      # 集成测试
├── setup.py              # 安装配置
├── pyproject.toml        # 项目配置
├── requirements.txt      # 依赖清单
└── README.md            # 本文件
```

---

## 🤝 贡献

欢迎贡献代码！请遵循以下步骤：

1. Fork 本仓库
2. 创建您的特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交您的更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开一个 Pull Request

### 代码规范

- 遵循 PEP 8 代码风格
- 添加必要的注释和文档字符串
- 确保所有测试通过
- 保持测试覆盖率在 70% 以上

---

## 📝 更新日志

### v2.1.0 (2024-11-17)

**重大更新 - 进度管理与交互体验提升** 💾

- ✅ **进度保存**：自动保存阅读进度，支持所有模式（终端、管道、跳转）
- ✅ **进度恢复**：重新打开文件自动从上次位置继续阅读
- ✅ **交互式分页器**：内置 vim 风格分页器，支持键位提示
- ✅ **完整文档访问**：跳转参数加载完整文档，可前后翻页
- ✅ **绝对路径管理**：任意目录访问都能正确匹配进度
- ✅ **精确进度计算**：基于 Paginator 分页，页码计算准确
- ✅ **状态栏显示**：实时显示当前位置、总行数、百分比
- ✅ 156 个测试用例全部通过

### v2.0.0 (2024-11-16)

**重大更新 - 性能优化与用户体验提升** 🚀

- ✅ **性能优化**：大幅提升大文档处理速度（360页文档从卡顿到0.5秒）
- ✅ **静默模式**：去除加载信息，退出分页器后屏幕干净
- ✅ **智能跳转**：跳转后默认输出到文档末尾，无需指定 `--pages`
- ✅ **流式输出**：管道/重定向时采用流式输出，降低内存占用
- ✅ **错误处理**：优雅处理管道关闭（BrokenPipeError）
- ✅ **批量优化**：一次性获取所有页面，避免重复遍历
- ✅ 支持 EPUB、MOBI、TXT、Markdown 格式
- ✅ 密码保护功能
- ✅ 跨平台支持
- ✅ 156 个测试用例全部通过

---

## 📄 许可证

本项目采用 [MIT License](LICENSE) 许可证。

---

## 🙏 致谢

- [ebooklib](https://github.com/aerkalov/ebooklib) - EPUB解析支持
- [mobi](https://github.com/iscc/mobi) - MOBI格式支持
- [chardet](https://github.com/chardet/chardet) - 编码检测支持
- [BeautifulSoup](https://www.crummy.com/software/BeautifulSoup/) - HTML解析支持
- 所有贡献者和用户


---

## 🔮 未来计划

- [ ] PDF 格式支持
- [ ] 更多输出格式选项（JSON、HTML等）
- [ ] 批量转换功能
- [ ] 目录提取和导出
- [ ] 元数据提取功能
- [ ] 书签管理功能
- [ ] 搜索功能（在分页器中搜索）
- [ ] 主题配色支持

---

<div align="center">

**如果觉得有用，请给个 ⭐️ 支持一下！**

Made with ❤️ by iBookRead Team

</div>
